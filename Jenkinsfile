pipeline {
    agent any
    tools {
        maven 'Maven 3.8.7'
    }
    stages {
        stage('Exemplo') {
            steps {
                // Build steps here, for example:
                bat "mvn clean compile"
            }
        }
      
    }
}

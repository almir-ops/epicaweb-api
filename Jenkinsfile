pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Build steps here, for example:
                sh 'mvn clean install'
            }
        }
        
    }
}
